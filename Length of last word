**
 * @param {string} s
 * @return {number}
 */
var lengthOfLastWord = function(s) {
    let number = 0;
    s = s.trim()
   for( i = s.length - 1; i >= 0; i--){
        if(s[i] === " "){
            break
        }
        number ++

   }

   return number
};
