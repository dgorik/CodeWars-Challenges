/**
 * @param {number[]} nums
 * @return {number}
 */
var missingNumber = function(nums) {
   let compare = 0
   while(compare < nums.length){
    if(!nums.includes(compare)){
        return compare
    }
    compare++
   }
   return nums.length
};

let x = missingNumber([3,0,1])
console.log(x)
